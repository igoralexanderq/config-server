server:
  port: 9099

spring:
  security:
    oauth2:
      client:
        provider:
          mitocode-realm:
            issuer-uri: http://localhost:8080/realms/mitocode-realm
          registration:
            mitocode-realm:
              provider: mitocode-realm
              client-name: mitocode-client
              client-id: mitocode-client
              client-secret: MYbNUshfBCKPu2OMCTpnDS6SlYw31rjd
              scope: openid
              authorization-grand-type: authorization_code
